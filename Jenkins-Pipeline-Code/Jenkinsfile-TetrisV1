pipeline {
    agent any
    
    tools {
        nodejs 'nodejs' // Jenkins "Global Tool Configuration"da shu nom bilan qo'shilgan bo'lishi kerak
    }
    
    stages {
        stage('Cleaning Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout from Git') {
            steps {
                // Loyihani GitHub-dan tortib olish
                git branch: 'main', url: 'https://github.com/ersuncoder/SecureCloud-K8s-DevSecOps.git'
            }
        }
        
        stage('Installing Dependencies') {
            steps {
                dir('Tetris-V1') {
                    // Node.js kutubxonalarini o'rnatish
                    bat 'npm install'
                }
            }
        }
        
        stage('Trivy File Scan') {
            steps {
                dir('Tetris-V1') {
                    // Fayllarni xavfsizlikka (vulnerabilities) tekshirish
                    bat 'trivy fs . > trivyfs.txt'
                }
            }
        }
        
        stage("Docker Image Build") {
            steps {
                script {
                    dir('Tetris-V1') {
                        // Docker Registry-ga ulanish (DockerHub credentials ID: 'docker')
                        withDockerRegistry(credentialsId: 'docker', toolName: 'docker') {
                            bat 'docker system prune -f'
                            bat 'docker build -t tetrisv1 .'
                        }
                    }
                }
            }
        }
        
        stage("Docker Image Pushing") {
            steps {
                script {
                    withDockerRegistry(credentialsId: 'docker', toolName: 'docker') {
                        // Image-ni yangi Build raqami bilan taglash va push qilish
                        bat "docker tag tetrisv1 ersun7/tetrisv1:${BUILD_NUMBER}"
                        bat "docker push ersun7/tetrisv1:${BUILD_NUMBER}"
                    }
                }
            }
        }
        
        stage("TRIVY Image Scan") {
            steps {
                // Yaratilgan Docker Image-ni xavfsizlikka tekshirish
                bat "trivy image ersun7/tetrisv1:${BUILD_NUMBER} > trivyimage.txt"
            }
        }

        stage('Update Deployment file') {
            environment {
                GIT_REPO_NAME = "SecureCloud-K8s-DevSecOps"
                GIT_USER_NAME = "ersuncoder"
            }
            steps {
                dir('Manifest-file') {
                    // GitHub-ga yozish uchun 'github' ID-li Secret Text ishlatiladi
                    withCredentials([string(credentialsId: 'github', variable: 'GITHUB_TOKEN')]) {
                        bat """
                            @echo off
                            :: 1. AVTOMATLASHTIRISH: deployment-service.yml ichidagi image versiyasini yangilash
                            powershell -Command "(Get-Content deployment-service.yml) -replace 'tetrisv1:[0-9]+', 'tetrisv1:${BUILD_NUMBER}' | Set-Content deployment-service.yml"

                            :: 2. Git sozlamalari
                            git config user.email "ermatovs426@gmail.com"
                            git config user.name "ersuncoder"
                            
                            :: 3. O'zgarishlarni GitHub-ga yuklash (Continuous Deployment asosi)
                            git add deployment-service.yml
                            git commit -m "Update deployment Image to version ${BUILD_NUMBER}"
                            git push https://%GITHUB_TOKEN%@github.com/%GIT_USER_NAME%/%GIT_REPO_NAME% HEAD:master
                        """
                    }
                }
            }
        }
    }
    
    post {
        always {
            echo 'Pipeline jarayoni yakunlandi.'
        }
        success {
            echo 'Muvaffaqiyatli yakunlandi: Barcha xavfsizlik va avtomatlashtirish bosqichlari o\'tdi!'
        }
        failure {
            echo 'Xatolik yuz berdi. Iltimos, loglarni tekshiring.'
        }
    }
}
